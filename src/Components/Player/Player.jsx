import PropTypes from "prop-types";

const Player = ({}) => { 
    return (
        <div>

            
        </div>
    );
};


Player.propTypes ={
    players: PropTypes.object.isRequired,
}
export default Player;